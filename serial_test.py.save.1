import serial

check = 0

connected = False
print "Not connected"
try:
	ser = serial.Serial("/dev/ttyACM1", 9600)
	pas = 1
	break
except:
	print"didn't work"
	pas = 0

if pas == 1:
	while not connected:
		serin = ser.read()
		connected = True
	print "Connected"

	ser.write("1")

	while check == 0:
		word = ser.readline()
		print word				if 'x' in word:
			check = 1
	
	ser.close()
