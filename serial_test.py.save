import serial

check = 0

connected = False
print "Not connected"

ser = serial.Serial("/dev/ttyACM0", 9600)

while not connected:
	serin = ser.read()
	connected = True
print "Connected"

ser.write("1")

while check == 0:
	word = ser.readline()
	print word
	if word == "x":
		ser.close
